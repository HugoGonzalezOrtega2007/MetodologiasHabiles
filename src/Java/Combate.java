package Java;
import java.util.Random;

public class Combate {
    private int vidaMaxima = 100;
    private int ataque = 15;
    private int esquivo = (int) (Math.random() * 10);
}

if (esquivo == 3) {
    sout("El personaje ha esquivado el ataque");
        } else {
    sout("El personaje ha recibido el ataque");
                }